name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    name: test
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run checks
        run: ./scripts/test.sh
